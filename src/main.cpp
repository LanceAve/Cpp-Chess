#include <iostream>
using namespace std;
#include "Board.hpp";

// This will follow the rank-file system (rows first, columns second)

int main() {
    Board board;
    board.printBoard();
    return 0;
}